{
  "name": "core-blog-vnext",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "generate": "node scripts/compile.cjs ./markdown ./data",
    "cleanup": "rm -rf data && mkdir -p data/blog && mkdir -p data/list",
    "build": "tsc && vite build && cp fake-jquery.js dist/ && cp -r public dist/ && cp -r data dist/data && cp -r extra dist/extra || :"
  },
  "devDependencies": {
    "showdown": "^2.1.0",
    "tsx-dom": "^2.0.0",
    "typescript": "^4.9.3",
    "vite": "^4.0.0"
  },
  "dependencies": {
    "dayjs": "^1.11.7"
  }
}
